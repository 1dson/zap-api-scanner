scan {
  reportName = "Penetration_Results"
  contextName = "zap-api-context"
}

api {
  call = ["get","post", "put"]
  baseURL = "https://example.com"
  apiKey = "somekey"
  resources = ["00200753", "SELECT%20DISTINCT%20%2A%20FROM%20%2A%20LIMIT%2010%3B", "create%20database%20s3_access_logs_db"]
  payload {
    put {
      "first": "example",
      "title": "json"
    },
    post {
      "second": "example",
      "title": "json"
    }
  }
}